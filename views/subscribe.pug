html
  head
    title Upgrade - AliasZap
    link(href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css', rel='stylesheet')
  body
    nav.navbar.navbar-expand-lg.navbar-light.bg-light
      .container
        a.navbar-brand(href='/') AliasZap
        button.navbar-toggler(type='button', data-bs-toggle='collapse', data-bs-target='#navbarNav')
          span.navbar-toggler-icon
        #navbarNav.collapse.navbar-collapse
          ul.navbar-nav.ms-auto
            li.nav-item
              a.nav-link(href='/dashboard') Dashboard
            li.nav-item
              a.nav-link.active(href='/subscribe') Upgrade
            li.nav-item
              a.nav-link(href='/logout') Logout
    .container.mt-5
      .row
        .col-md-6.offset-md-3.text-center
          h2.mb-4 Upgrade to Basic
          p For just $3/month, get unlimited aliases and priority support.
          if user.plan === 'basic'
            p.text-success You’re already on the Basic plan!
            a.btn.btn-secondary.mt-3(href='/dashboard') Back to Dashboard
          else
            form(method='POST', action='/subscribe')
              input(type='hidden', name='_csrf', value=csrfToken)
              button.btn.btn-primary.btn-lg(type='submit') Upgrade Now
    script(src='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js')